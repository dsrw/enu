#? stdtmpl
#proc build_code_template(file_name, imports: string): string =
import std / strutils
import types, class_macros, players, state_machine, base_api, bots, builds, private_builds, helpers

let instance_global_by_default = false
var move_mode = 0

${imports}

{.experimental: "overloadable_enums".}

proc echo(msg: varargs[string, `$$`]) = echo_console msg.join

load_enu_script "${file_name}", Build, position, speed, scale, energy, global, drawing, color, initial_position

exit()
