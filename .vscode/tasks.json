{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "command": "nimble",
            "args": ["build", "--verbose"],
            "type": "shell",
            "label": "nim-build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
